import { Button } from "@mui/material";

function FinalizarButton() {
  return (
    <Button variant="outlined" size="large" fullWidth sx={{ padding: 3 }}>
      Finalizar
    </Button>
  );
}

export default FinalizarButton;
